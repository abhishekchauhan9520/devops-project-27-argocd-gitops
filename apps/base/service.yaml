apiVersion: v1
kind: Service
metadata:
  name: gitops-demo
  labels:
    app: gitops-demo
spec:
  type: ClusterIP
  selector:
    app: gitops-demo
  ports:
    - name: http
      port: 80
      targetPort: http
